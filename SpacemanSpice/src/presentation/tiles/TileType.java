package presentation.tiles;

/**
 * Enum types of tiles, based on its appearance.
 * 
 */
public enum TileType {
    FLOOR, WALL, CONSOLE, NOTHING, DOOR,
    NORTH_EXIT, SOUTH_EXIT, WEST_EXIT, EAST_EXIT
}
